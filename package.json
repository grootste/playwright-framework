{
  "name": "covergo",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "test:parallel": "npx playwright test tests/specs/10_testcase.spec.js --workers=4",
    "test:sequential": "npx playwright test tests/specs/10_testcase.spec.js --workers=1"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
